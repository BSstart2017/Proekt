import React from 'react';
import preLoader from "../../../assets/img/Preloader.svg";


let PreLoader = (props) => {
    return <div>
        <img src={preLoader} />
    </div>
}

export default PreLoader;